# 应用名称
spring:
  application:
    name: zuul

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultzone: http://localhost:8761/eureka

#公开所有的端点
management:
  endpoints:
    web:
      exposure:
        include: "*"
